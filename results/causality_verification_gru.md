# Causality verification

Generated by `python scripts/verify_causality.py`. Each check perturbs the input at a
future time and requires every earlier value to be **bit-identical**, not merely close.

- Generated: 2026-09-10 18:58:09 UTC
- Config: `configs\sleep78_streaming_gru.yaml`
- Normalization: `causal_rolling`, trailing window 30 s
- Model weights: randomly initialised weights
- Probe: 240 s of synthetic EEG at 100 Hz, perturbed from second 120
- Processed subject checked: not used

| Check | Result | Detail |
|---|---|---|
| Normalization ignores future samples | **PASS** | max |difference| before sample 12,000 = 0.000e+00 |
| Offline arrays equal the online, sample-at-a-time filter | **PASS** | max |offline - streaming| = 5.240e-14 over 24,000 samples |
| Normalized output is finite through the cold start | **PASS** | max |z| = 4.932, bound sqrt(window-1) = 54.763 |
| Configuration declares a causal model | **PASS** | `causal: true` |
| Raw sample -> normalization -> model: no future leakage | **PASS** | max |logit difference| for seconds 0-119 = 0.000e+00 (bit-identical required) |
| The probe actually perturbs later outputs (test is not vacuous) | **PASS** | seconds 120+ differ as expected |
| Non-causal control does leak (so the ablation is a real contrast) | **PASS** | removing the masks and left-padding lets future signal reach earlier outputs |

**All checks passed.** For this configuration the prediction at second *t* is a
function of the raw signal up to second *t* only, through both preprocessing and the
network, and the offline arrays used for training are exactly what a sample-at-a-time
streaming implementation produces.

