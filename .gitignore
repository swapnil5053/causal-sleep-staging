# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
.venv/
env/
.env/
ENV/

# Jupyter Notebook
.ipynb_checkpoints

# Project specific
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep
checkpoints/*
!checkpoints/.gitkeep

# OS / Editors
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Run artifacts (curated copies live in results/)
logs/
train_log*.txt
RUN_GUIDE.md
CONSISTENCY_NOTES.md

# Ablation run outputs
logs_run*/
checkpoints_run*/

# Every other generated data / log / checkpoint directory (78-subject, streaming, ablations).
# Curated copies are committed from results/ instead.
data/raw*/
data/processed*/
logs_*/
checkpoints_*/
*_run.log
run_*.log
overnight_run.log

# Internal working notes, panel-prep material and locally generated reports.
# None of this belongs in the public repository the paper cites.
notes_internal/
Claude outputs/
figures/deck/
results/generated/
logs_sweep_runs/
logs_sweep_*/
checkpoints_sweep_*/

# DOD-H / DOD-O raw archives and extracted h5 (tens of GB, never committed)
data/dod*/

# Unattended run transcripts
*.log

BRANCH_NOTES.md
